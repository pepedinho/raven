[package]
name = "raven"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
async-trait = "0.1.89"
toml = "0.8"

# mlua = { version = "0.9", features = ["lua54", "send", "async"] }

raven-logger = {path = "../raven-logger"}